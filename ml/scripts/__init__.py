"""Operational scripts for the WeaveSelf Python package (Track A/B).

These are runnable entrypoints (e.g. ``run_weave_eval``) rather than importable
library code; they live outside ``weaveself/`` so they are not packaged.
"""
